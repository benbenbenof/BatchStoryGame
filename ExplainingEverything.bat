REM So we set a variable using the set /a and letting the player decide the parameters of the variable
REm we use goto to call a variable action (:a)
Rem echo is like print function in python
rem title is title
rem that should explain something